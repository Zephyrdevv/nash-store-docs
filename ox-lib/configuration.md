# Configuration

## ConVars

| ConVar | Default | Description |
|--------|---------|-------------|
| ox:primaryColor | Nash theme | Primary UI accent color (hex) |
| ox:errorColor | #E74C3C | Error state color |
| ox:defaultLocale | en | Default locale |

The Nash Edition applies a custom color palette baked into the NUI build.
