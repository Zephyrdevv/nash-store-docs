# QBOX

QBOX is a fork of QBCore. Nash Banking treats it as a separate detection target to handle the cases where `qbx_core` ships with a slightly different API than `qb-core`.

## Auto-detection

QBOX is detected when `qbx_core` is started.

## Differences from QBCore

_Notable API differences — filled in Pass 2._

## Known quirks

_Anything QBOX-specific — filled in Pass 2._
