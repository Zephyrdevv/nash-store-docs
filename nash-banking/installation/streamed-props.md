# Streamed Props

Nash Banking ships with two streamed archetypes:

- A **bank card** prop, attached to the player's hand during ATM sessions and card inspections.
- A **payment terminal** prop, used by the TPE item.

## What's already set up

The `fxmanifest.lua` of `nash_banking` already declares:

```lua
data_file 'DLC_ITYP_REQUEST' 'stream/bzzz_prop_payment_terminal.ytyp'
data_file 'DLC_ITYP_REQUEST' 'stream/nash_banking.ytyp'
```

## Files in `stream/`

_Description of each file in the `stream/` folder and what it does — filled in Pass 2._

## Troubleshooting

_What to check if the card or TPE prop does not spawn in the player's hand — filled in Pass 2._

## Credits

The card props are based on **Alcaline's Wallet Pack**.
