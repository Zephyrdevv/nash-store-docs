# Global Exports

## addGlobalVehicle(options)
Adds options to all vehicles.

## addGlobalPed(options)
Adds options to all peds.

## addGlobalObject(options)
Adds options to all objects.

## addGlobalPlayer(options)
Adds options to all players.

## removeGlobalOption(name)
Removes a global option by name.
