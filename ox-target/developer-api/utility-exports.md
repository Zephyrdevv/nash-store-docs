# Utility Exports

## disableTargeting(state)
Enables or disables the targeting system.

| Parameter | Type | Description |
|-----------|------|-------------|
| state | boolean | true to disable, false to enable |

## getTargetedEntity()
Returns the currently targeted entity handle, or 0 if none.

## setDrawDistance(distance)
Overrides the draw distance at runtime.
